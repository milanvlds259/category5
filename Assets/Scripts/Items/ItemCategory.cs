namespace Category5.Items
{
    // category for future gear-system pivot
    // not enforced for now, but tracked on items
    public enum ItemCategory
    {
        General,    // can go in any slot (default)
        Helmet,     // future: head slot only
        Armor,      // future: chest slot only
        Boots,      // future: feet slot only
        Accessory,  // future: accessory slots only
        Weapon      // future: weapon slot only
    }
}
